# 08 — Platform Constraints & Risk Register

## Platform capability matrix (DGX Spark: GB10, sm_121, CUDA ≥13, aarch64)

| Capability | Spark status | Confidence | Tracked as |
|---|---|---|---|
| Isaac Sim rendering (procedural + splat) | Runs on Spark | Version to confirm | `RISK-01` |
| Isaac Lab RL/IL training (small runs) | Runs on Spark; large-scale training bursts out | Version to confirm | `RISK-03` |
| PX4 SITL (the autopilot itself) | **Runs natively on aarch64** via the official `px4io/px4-sitl` arm64 container; simulator seam open on TCP 4560 | **Verified on this box** — `tools/px4_sitl_smoke.py` | `RISK-02`(a) closed |
| Pegasus Simulator (the Isaac-side bridge) | Needs a port: no release targets Isaac 6.x, and `omni.isaac.dynamic_control` is absent from this build | **Measured** — 21/26 of its imports resolve; 17 call sites to port | `RISK-02`(b), `RISK-26` |
| PhysX force fields (wind/drag/noise) | **Absent from this build.** `omni.physx.forcefields` is not among the 18 `omni.physx.*` extensions, and `ForceField` appears in zero `.py`/`.toml` files across all 473 — the USD schema is gone too, so this is not a disabled extension | **Measured 2026-07-29** — worked around in pure python (`world/windfield.py`) | `RISK-27` |
| Sensor RTX / `ovrtx` | aarch64 wheel exists; runtime on sm_121 unconfirmed | Plausible | `RISK-05` |
| Cosmos Reason (2B/8B) inference | Runs via mainline vLLM (NIM crashes on sm_121) | **Verified live on this box** per `mission.yaml` comments | `RISK-04`, `RISK-09` |
| Cosmos Transfer (generation) | **Confirmed unsupported** | Non-negotiable, off-box | — (design constraint, not open) |
| Cosmos Predict (2B) | Blackwell+ARM inference support claimed (v1.3.3) | Unverified on this Spark | `RISK-04` |
| 3dgrut (NuRec/3DGUT reconstruction) | CUDA 13.0 "experimental"; arm64 Docker build exists | Untested on sm_121 | `RISK-06` |
| cuOpt (routing) | Apache-2.0, GPU solver | aarch64/GB10 build unverified | `RISK-10` |
| Isaac ROS (cuVSLAM, nvblox, Perceptor, NITROS) | **Supported since 4.2.0** (nvblox explicitly lists DGX Spark) | Confirmed for prototyping | — |
| Mission Dispatch / Mission Database | **Not yet Spark-supported** | Confirmed gap | `RISK-08` |
| ROS 2 sensor image topics (sim-side) | Reported rendering quirks | Why sim-native is default | `RISK-21` |

## Risk register

Each entry: description, what it blocks, the action that resolves it, and
current status. "Blocks" cites the `FR`/`SLICE`/`HAZ`/`NFR` it gates.

| ID | Description | Blocks | Verification action | Status |
|---|---|---|---|---|
| `RISK-01` | Isaac Sim version tension: `CLAUDE.md` pins 5.1 (source build); the research brief assumes 6.0.1 (GA'd 2026-06-04). NuRec render path, Sensor RTX/`ovrtx` API, and Pegasus (built for 5.1) compatibility all differ across this boundary. | All of `PIL-1`, `PIL-2`; every slice from `SLICE-2` on | Confirm the installed build on the actual Spark; reconcile `CLAUDE.md` in the same commit if it's wrong (`NFR-10`) | Open |
| `RISK-02` | **Investigated 2026-07-29 and it splits in two — the PX4 half is closed, the Isaac-side half is quantified.** (a) **PX4 SITL on aarch64: RESOLVED.** Not by building Pegasus's pinned PX4 v1.14.3 from source (a 2023 release on a 2024 distro on an unlisted architecture), and not from PX4's advertised Ubuntu-24.04-arm64 `.deb`s (the tagged releases carry only a VOXL board package) — but from the **official multi-arch container `px4io/px4-sitl`**, which publishes a real `linux/arm64` manifest. Measured: 119 MB, native aarch64 ELF, boots to `INFO [simulator_mavlink] Waiting for simulator to accept connection on TCP port 4560`, and that port is reachable from the host. Reproducible via `tools/px4_sitl_smoke.py` (exit 0 = seam open). (b) **Pegasus on Isaac 6.0.1: needs a port, and the port is bounded.** No Pegasus release targets Isaac 6.x — v5.1.0 (2025-10-26) targets 5.0/5.1 and its docs specify Ubuntu 22.04/x86_64. Probed all 26 of its `omni.*`/`isaacsim.*` imports inside a real headless 6.0.1 session: **21 resolve**. Of the 5 that fail, 3 (`isaacsim.ros2.bridge`, `isaacsim.replicator.agent.core`, `omni.anim.graph.core`) are **present on disk and merely not enabled** — not port work. Only 2 are genuinely gone: `omni.isaac.sensor` (peripheral — Pegasus's ROS 2 camera graph) and **`omni.isaac.dynamic_control`**, which is load-bearing. The entire legacy `omni.isaac.*` namespace is absent from the build except `omni.isaac.core_archive`. | `FR-06`–`FR-09`, `SLICE-2` | The `dynamic_control` port is **17 call sites in 2 files** (`vehicle.py`, `multirotor.py`), funnelled through one accessor `Vehicle.get_dc_interface()`, using 10 distinct methods: rigid-body handle/pose/velocity reads, `apply_body_force`/`apply_body_torque`, and articulation DOF velocity (the *visual* rotor spin only). All map onto `isaacsim.core.prims` / `omni.physics.tensors`, **both verified present on 6.0.1**. Recommended: a time-boxed fork-and-patch spike, because Pegasus's real value is the multirotor dynamics + HIL sensor models (IMU/GPS/baro/mag) that PX4's EKF needs — writing those from scratch is where drone-sim efforts sink. Fall back to staying kinematic (`FR-07` keeps that valid) if the patch does not converge. | (a) **Resolved 2026-07-29**; (b) **Port DONE and verified 2026-07-29** — `tools/install_pegasus_isaac6.sh` + `tools/patches/pegasus-v5.1.0-isaac6.patch` make Pegasus v5.1.0 import and drive physics on 6.0.1 (`dc_compat.py` reimplements the retired surface on `isaacsim.core.prims`; reads matched a direct prim read on a falling Iris). The estimate held: 2 files touched, ~270 lines added, upstream call sites unchanged. Remaining work is the PX4 loop itself → `RISK-28` |
| `RISK-26` | **Pegasus's MAVLink backend was written against PX4 v1.14.3; the aarch64 container ships ~v1.18.0-beta1** (image built 2026-07-08, Ubuntu 24.04 base). `px4_mavlink_backend.py` speaks the HIL protocol (sensor/GPS/state messages) and PX4's lockstep handshake, neither of which is guaranteed stable across four minor releases. Untested — the bridge does not exist yet, so there was nothing to test it with. | `FR-06`, `FR-08`, `SLICE-2` | Verify the handshake before trusting any flight behaviour: connect to TCP 4560 (`tools/px4_sitl_smoke.py --keep`) and confirm PX4 accepts the HIL stream and leaves `Waiting for simulator`. If the protocol has drifted, either pin an older container tag or update the backend — decide from the observed failure, not by assuming. | Open — new, discovered by this investigation |
| `RISK-03` | Isaac Lab version: `CLAUDE.md` says "Isaac Lab 3.0"; the current stable release is 2.3.0 (built on Isaac Sim 5.1), while 3.0.0-beta exists as a beta (~July 2026). | `PIL-4`, `SLICE-5` | Confirm the actual installed version against `github.com/isaac-sim/IsaacLab/releases` | Open |
| `RISK-04` | Cosmos generation/reasoning footprint on GB10: Cosmos Transfer is confirmed unsupported; the Reason NIM crashes on sm_121 (served via vLLM as the workaround — verified live per `mission.yaml`). Unconfirmed: whether Predict2.5-2B, a distilled Transfer variant, or Cosmos 3 Edge/Nano can run locally, and whether a newer Reason2/Cosmos 3 NIM now supports sm_121 (superseding the vLLM workaround). | `FR-02`, `FR-18`, `PIL-3`, `PIL-5`, `NFR-05` | Test each candidate model/variant on GB10 before assuming strictly off-box; re-check NIM sm_121 support periodically | Open (vLLM Reason2 path partially resolved; generation confirmed off-box) |
| `RISK-05` | Sensor RTX / `ovrtx` ships an aarch64 wheel (`manylinux_2_35_aarch64`), but the API was early-access in 2025 ("Omniverse Cloud Sensor RTX APIs"). Runtime behavior on sm_121/GB10 is unconfirmed. | `FR-11` (blade shadow rendering), `HAZ-07`, `SLICE-3` | Confirm the current `ovrtx` release actually runs on sm_121/GB10 before relying on it for the false-fault harness | Open |
| `RISK-06` | 3dgrut's supported CUDA versions include 13.0 flagged **experimental**; an arm64 Docker build is published but untested on sm_121/GB10. | `FR-19`, `PIL-1`, `SLICE-6` entry criterion | Verify a working CUDA-13/aarch64/sm_121 3dgrut build on the Spark; if it fails, formally designate reconstruction as burst-out (this is the `SLICE-6` decision gate) | Open |
| `RISK-07` | 3dgrut's custom NuRec USDZ export is slated for deprecation, to be replaced by "ParticleField." Scripting the reconstruction pipeline against the wrong export format wastes the work. | `FR-19`, `SLICE-6` | Confirm the current target export format immediately before scripting the capture→COLMAP→3DGUT pipeline, not at design time | Open |
| `RISK-08` | Isaac ROS supports the Spark since 4.2.0, but **Mission Dispatch / Mission Database containers are not yet Spark-supported**. This directly affects prototyping the fleet-command loop on-box. | `FR-23`, `PIL-6`, `SLICE-7`/`SLICE-8` | Plan the fleet-command loop assuming off-box or a supported Jetson until Spark support lands; re-check Isaac ROS release notes each cadence | Open — **gating risk for `SLICE-7`** |
| `RISK-09` | Cosmos Reason inference latency/throughput on GB10 via vLLM is unbenchmarked at actual inspection frame rates (screen + confirm passes per panel). | `NFR-06`, `KPI-02`, `KPI-06` | Benchmark end-to-end latency per panel under the vLLM path; feed the number into coverage/time-window KPI budgets | Open |
| `RISK-10` | cuOpt's aarch64/GB10 build is unverified; additionally, `orchestrator/routing.py` (`IF-05`) assumes the cuOpt Python client has no Isaac/GPU-driver dependency that would violate the Isaac-free boundary (`NFR-01`). | `FR-22`, `IF-05`, `SLICE-7` | Verify the cuOpt build on this Spark and confirm the client package imports cleanly without Isaac before writing `orchestrator/routing.py` | Open |
| `RISK-11` | Articulated-turbine collider cooking (mesh/convex colliders on tower/nacelle/blades) has not been verified against the installed Isaac build. Colliders are now **authored** in `farm_builder.py` (commit `7894eb3`) but are inert under kinematic teleport and their cooking/firing is untested; the turbine is still a kinematic Xform, not a driven articulation. | `FR-11`, `HAZ-01`, `SLICE-2` | Convert the proxy to a real articulation and confirm collision fires against a dynamic body before relying on it; the planning-layer keep-out (`IF-07`) is the safety net until then | Open |
| `RISK-12` | No CFD reference data exists to validate the parametric wake model's turbulence-intensity parameters against a real turbine — the wake model is a knowing approximation, not a validated one. | `FR-13`, `HAZ-02` | Best-effort: compare qualitative behavior (near-wake intensity growth, downstream decay) against any published reference turbine wake data; document as an approximation regardless | Open (accepted as a permanent fidelity trade-off, not fully resolvable on this platform) |
| `RISK-13` | Documented PhysX forum issue: applying a force field to an articulation link throws `"PxArticulationLink - Articulation Link must be in a Scene"`. The drone is an articulation, so wind/wake force fields may hit this directly. | `FR-12`, `FR-13`, `SLICE-2` | Reproduce or avoid on the installed build; confirm the correct attach-order/schema before shipping `SLICE-2` | Open |
| `RISK-14` | Trimesh terrain has a documented PhysX mesh-cooking/fall-through failure mode (Isaac Lab issue #2323, now closed upstream, but not validated on our own DEM). | `FR-10`, `HAZ-04`, `SLICE-2`/`SLICE-6` | Validate contact behavior on our specific procedural terrain and, later, the real DEM — don't assume the upstream fix generalizes | Open |
| `RISK-15` | Bird trajectory realism is an open design choice: procedural/scripted trajectories vs. a recorded or learned motion set. Affects how meaningful `HAZ-05` testing actually is. | `FR-14`, `SLICE-5` | Decide at `SLICE-5` kickoff based on available data; document the choice as a fidelity trade-off either way (`NFR-07`) | Open |
| `RISK-16` | Whether Cosmos Transfer preserves geometrically-correct blade-shadow motion or hallucinates it is unconfirmed — this is the single highest-consequence unknown for `NFR-08`/`FR-05`, since a hallucinated shadow would poison exactly the fault ground truth the project protects. | `FR-05`, `NFR-08`, `HAZ-07`, `SLICE-4` | Spot-check generated shadow geometry/motion against the known turbine articulation state before trusting any generated corpus for training or evaluation | Open — **high priority** |
| `RISK-17` | SIL→HIL KPI-parity tolerance for `SLICE-8` is undefined — there is currently no number for "how close is close enough" between a twin run and a real-robot run on the same scenario. | `FR-24`, `SLICE-8` | Define the tolerance with input from whoever owns acceptance for real flight, before `SLICE-8` exit criteria are enforced | Open |
| `RISK-18` | ROS 2 distro target (Humble vs. Jazzy) is inconsistent across Isaac ROS package docs for the specific packages we'd deploy. | `FR-23`, `PIL-6` | Confirm the exact distro per package before wiring `ros2_bridge.py`'s dependencies | Open |
| `RISK-19` | The canonical fleet-command repo is unsettled among `isaac_ros_mission_client` / `isaac_mission_dispatch` / `isaac_mission_control` / `isaac_ros_cloud_control` — the lineage is genuinely in flux. | `FR-23`, `SLICE-7`/`SLICE-8` | Confirm the canonical, currently-maintained repo immediately before integration work starts, not at design time | Open |
| `RISK-20` | cuVSLAM robustness over repetitive panel rows (visual aliasing) and in GPS-degraded, wind-buffeted flight is unverified for this specific site geometry. | `FR-24`, `SLICE-8` | Test cuVSLAM against a recorded or simulated repetitive-row traverse before trusting it as the on-robot pose source | Open |
| `RISK-21` | Reported ROS 2 sensor-rendering quirks on the Spark are the stated reason sim-native `Transport` is the default. Sim-side ROS 2 image topics should not be trusted without verification. | `NFR-05`, `SLICE-8` | Re-verify before any `SLICE-8` work depends on sim-side ROS 2 image publishing | Open |
| `RISK-22` | Jetson AGX Thor module (T5000) pricing (~$2,999/1k units reported) and exact SKU for the robot BOM are unverified secondhand figures. | Deploy BOM planning only — not a technical blocker | Confirm current pricing/SKU with NVIDIA before BOM commitment | Open (low priority, non-technical) |

| `RISK-23` | **A KPI measured through the VLM is only reproducible while inference is served serially.** Measured 2026-07-28 on this box (vLLM, `nvidia/cosmos-reason1-7b`, one real camera frame): 15/15 byte-identical responses issued serially — even *without* a seed — but 4 identical requests issued CONCURRENTLY returned 2× `soiled` and 2× `healthy`. Continuous batching changes the arithmetic and no request-level parameter (`temperature`, `seed`, `top_k`) fixes it. The mission FSM is serial today, so any future parallel screening would silently make every KPI non-reproducible. | `FR-02`, `FR-03`, `FR-17`, `NFR-02`, `KPI-01`, `KPI-03` | Decoding is pinned greedy and recorded per run (`cosmos_reason.DEFAULT_SAMPLING` → the run record's `perception.sampling`); `run.py --repeat N` quotes a spread instead of a number and attributes each disagreement to renderer vs. model by frame digest. **Before any batched/parallel inference path is added, re-measure and state the variance it introduces.** | Open — mitigated for serial runs, unresolved for batched ones |
| `RISK-24` | **RTX capture is NOT bit-reproducible — not even from a camera that never moves.** Measured 2026-07-28 (`tools/probe_render_determinism.py`, 640x480, low-sun Khavda stage): 4 captures held still → **4 distinct digests**, 0.85/255 mean pixel difference, ~52% of pixels changed; leave-and-return is worse (2.39 mean, 88% of pixels); 10 extra settling steps do **not** converge it (0.70 mean). A 3-repeat mission run agreed: **40/40 panels rendered differently between repeats.** So the Session-10b verdict flip needs no exotic explanation, and — critically — bit-exact digests cannot attribute a flip, because they *always* differ in a rendered run. | `NFR-02`, `KPI-01`, `KPI-03` | **Resolved as a measured platform property, and designed around.** The amplitude is what matters: block-averaged into an 8x8 thumbnail, the same scene differs by **0.20–0.59 LSB** while a genuinely different picture (shaded panel vs. the unshaded control) differs by **35.5 LSB** — a ~60x gap. `perception.base.frame_thumbnail` + `kpi.variance.thumbnails_differ` (tolerance 1.0) therefore attribute flips on "same *picture*", not "same bits", and both counts are reported. A quantised *hash* was tried first and rejected by measurement: 256 blocks at 16 levels still flipped a boundary on ~3 of 4 unchanged captures. | **Resolved 2026-07-28** — renderer is stochastic at sub-LSB amplitude; attribution uses a measured tolerance, and any repeat set showing a *materially* different picture is flagged as not comparing like with like |

| `RISK-25` | **Verdicts are not stable under sub-perceptual input noise — and this is the mechanism behind the flips, not decoding.** Given identical input *bytes* Reason-1 is byte-repeatable (`RISK-23`). But the renderer never delivers identical bytes (`RISK-24`), and that difference — imperceptible at picture level, ~1.3 LSB on an 8x8 thumbnail — was enough to change a verdict. Measured over two 3-repeat sets on `demo_video` (24 panels, 7 escalations each): `R258-C013` (injected `soiled`) → `soiled/hotspot/hotspot` in set A; `R258-C014` (injected `hotspot`) → `healthy/healthy/soiled` in set B. The model's own notes describe genuinely different readings ("opaque tan or brown patch… soiling" vs. "a small, bright red/orange spot… localized overheating"), so it is confident and wrong, not hedging. Both flips are on **faulted** panels, i.e. this is `KPI-01` fragility; `KPI-03` was 0.000 and stable across all six runs. Note the taxonomy definitions in `_STATE_DEFINITIONS` were added to fix exactly this soiled↔hotspot confusion — they reduced it, they did not remove it. | `FR-02`, `FR-03`, `KPI-01`, `NFR-06` | Not fixable by pinning decoding (already greedy) and arguably **should not** be "fixed" in the twin: a real camera has sensor noise, so a model that flips on it would flip in the field too. Treat it as a genuine perception-robustness requirement — quantify with `--repeat` (per-panel agreement is reported), and drive it down with prompt/model work or ensembling rather than by making the renderer deterministic. `KPI-01` on this stage is 0.875 with **2 of 3 misses stable across repeats**, so the headline is a discrimination problem first and a variance problem second. | **Open — and prompt engineering has been measured and eliminated as the fix (2026-07-29).** Two interventions on `SC-01` (40 panels, N=3, majority vote): removing the `soiled` definition's "often heaviest at the panel's lower edge" cue plus a module-boundary instruction (`v2`), then restoring the cue and keeping only the boundary rule (`v3`). Both LOST: `detection_rate` 0.900 (0.875-0.900) -> 0.850 -> 0.825 (0.800-0.850), and both introduced a systematic error v1 did not have — **4 panels injected `soiled`, detected `soiled/soiled/soiled` under v1, came back `hotspot/hotspot/hotspot`**. `v3` falsified the obvious diagnosis: with the cue restored verbatim the same four still failed, so the descriptor was never the cause — the **boundary instruction** was. Telling the model that sandy discoloration near the module edge "is never a fault" suppresses the `soiled` class, because that is what soiling looks like, and it then reaches for `hotspot`. **Root cause: the false alarms and the true soiled detections rest on the same visual evidence** — sand-coloured pixels at the panel's lower edge, in a frame that also contains real desert ground. No wording separates them, which is why both attempts traded error classes rather than reducing error. ⚠ Also measured: `v3` had the BEST per-panel agreement of the three (0.950 vs v1's 0.875) and the worst accuracy — stability and accuracy are separate axes, so a fix can make the model confidently wrong the same way every time and look like progress. **Next move is the FRAME, not the prompt:** crop/mask capture to the module's own bounding box so ground is not in the image, then re-measure the same way. Prompt is pinned in the run record as `perception.prompt_version` so this is never invisible again |
| `RISK-27` | **`omni.physx.forcefields` does not exist on this build, and `FR-12` names it as the mechanism.** Measured 2026-07-29 against Isaac Sim 6.0.1-rc.7 / PhysX 110.1.13: of **473** extensions in `extscache`, 18 are `omni.physx.*` and none is `forcefields`; the string `ForceField` appears in **zero** `.py`/`.toml` files anywhere in the install, so the USD schema (`PhysxSchemaPhysxForceField*`) is gone too — this is not a disabled extension, it is absent. PhysX 110's renumbering appears to have dropped it. | `FR-12`, `FR-13`, `FR-08`, `KPI-05`, `SLICE-2` | **Worked around, and the workaround is arguably the better design.** `FR-13` was never satisfiable by a generic Wind field anyway — it asks for a Jensen/Gaussian *velocity-deficit profile localised to the downstream volume*, which is a specific model, not a uniform breeze. So `world/windfield.py` authors the field in **pure python**: ambient wind, seeded gusts (speed + direction variation per `FR-12`), Jensen top-hat wake with Katic root-sum-square combination, and a relative-velocity drag law. Being Isaac-free makes it unit-testable (38 tests) and — the part that matters here — **seeded and reproducible**, which any KPI measured under gust has to be. ⚠ What remains is the thin Isaac-bound *applier* that turns velocity into a per-body force each step; its API names need verifying against a live sim rather than recalled (`CLAUDE.md`), so it is deliberately not written yet. | Open — model layer built and tested; force-application layer needs a live sim to verify. `FR-12`/`FR-13` stay Partial |

| `RISK-28` | **A PX4-governed hover is not yet demonstrated, and the last mile has a known trap.** The port is verified at the physics level, but nothing has yet flown: no PX4 SITL connection, no armed vehicle, no station-keep. Two concrete unknowns remain. (i) **Pegasus's physics callbacks proved order-sensitive in a standalone app.** Its `Vehicle` registers four (`state`, `update`, `Sensors`, `mav_state`), and in some bootstraps the vehicle state froze at its spawn pose for 120 steps while the body was verifiably falling — yet in another run of the same script the state advanced correctly. A plain `World.add_physics_callback` fires reliably (22/20 steps, measured), and `update_state` produces correct values when called by hand, so the port is not the cause; the bootstrap ordering is. (ii) `RISK-26`'s protocol drift is still unexercised. | `FR-06`, `FR-08`, `SLICE-2` | Build the loop in the order that fails loudest: (1) pin down the callback-ordering condition — construct the vehicle *after* `world.reset()`/`play()` and compare, since prim views bind correctly only once the physics view exists; (2) start PX4 with `tools/px4_sitl_smoke.py --keep` and attach `PX4MavlinkBackend`, watching PX4's log leave `Waiting for simulator` (that single line settles `RISK-26`); (3) only then arm and judge a hover against `KPI-05`. Do NOT wrap this in `RobotControl` until a hover holds — an unstable `control/px4.py` behind the ABC would look like an orchestration bug. | Open — the real remaining `FR-06` work |

## Register hygiene

- When a `RISK-nn` resolves, update its **Status** in place (`Resolved
  YYYY-MM-DD — <what was confirmed>`); do not delete the row — it's the audit
  trail for why a design decision was made the way it was.
- When a new ⚠-verify item surfaces during implementation, add it here with
  the next sequential ID and link it from whichever requirement/hazard/slice
  it blocks — don't let open questions live only in code comments or PR
  descriptions.
- `RISK-02`, `RISK-06`, and `RISK-08` are called out as **gating risks** in
  `07-roadmap-and-milestones.md` because their slice cannot start design work
  in earnest until they're resolved one way or the other — everything else is
  a parallel-track verification.
